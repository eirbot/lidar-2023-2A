//#include "mbed.h"
#include "tools.h"

void Error_Handler(){
    while (1) {
//        std::string str = "Error\n";
//        pc.write(str.c_str(), str.length());
//        ThisThread::sleep_for(2s);

    }
}